# Upstream Sync State

Machine-readable-ish state for the `upstream-sync` workflow.
Upstream = `pingdotgg/t3code` (this Fenrir repo is a hard fork that has diverged).
The clone lives at `references/t3code` (remote `origin` -> upstream).

## Last evaluated upstream SHA

```
(none yet — first run looks back over a recent window)
```

## Last run summary

(empty)
