import Foundation

enum NativeDistributionLayerNotes {
    static let composition = "Live distribution probes stay behind service ports so startup UX can show actionable diagnostics without shelling out from product actions."
}
