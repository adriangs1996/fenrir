import Foundation

enum NativeDistributionViewNotes {
    static let startupFailureUX = "Startup diagnostics should be surfaced through the native shell degraded-state overlay before opening a local workspace."
}
