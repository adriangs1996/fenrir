import Foundation
import FenrirNativeShared

extension WorkspaceCoordinator {
    enum LayerBoundary {
        static let windowContracts = "NativeHost window contracts are ports; AppKit classes stay outside."
    }
}
