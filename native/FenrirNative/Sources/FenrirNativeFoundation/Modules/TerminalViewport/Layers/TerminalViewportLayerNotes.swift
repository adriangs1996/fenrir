import Foundation
import FenrirNativeShared

extension TerminalViewport {
    enum LayerBoundary {
        static let renderer = "libGhostty adapters must implement TerminalRenderer ports only."
    }
}
