import Foundation
import FenrirNativeShared

extension TerminalViewport {
    enum ViewBoundary {
        static let appKitHost = "AppKit views are owned here and exposed through DTO-free hosting ports."
    }
}
