import Foundation
import FenrirNativeShared

extension WorkflowControl {
    enum ViewBoundary {
        static let owner = "Workflow control views present server-run state without executing workflows locally."
    }
}
