import Foundation

public extension AgentIntegration {
    static let viewNotes = """
    AgentIntegration owns no transcript, response renderer, model picker, or
    approval panel. UI consumers should render advisory presence and
    integration status only.
    """
}
