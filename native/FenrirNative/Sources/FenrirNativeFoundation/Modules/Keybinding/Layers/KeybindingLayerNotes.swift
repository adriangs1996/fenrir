import Foundation
import FenrirNativeShared

extension Keybinding {
    enum LayerBoundary {
        static let storage = "Live keymap loading stays behind Keybinding services and is not public API."
    }
}
