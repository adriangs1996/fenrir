import Foundation
import FenrirNativeShared

extension Notifications {
    enum ViewBoundary {
        static let owner = "Notification UI projects attention state without owning workspace shell layout."
    }
}
