import Foundation
import FenrirNativeShared

extension Notifications {
    enum LayerBoundary {
        static let delivery = "Live notification delivery stays behind Notifications services and is not public API."
    }
}
