import Foundation
import FenrirNativeShared

extension WorkspaceOverlays {
    enum LayerBoundary {
        static let composition = "Live overlay presentation stays behind WorkspaceOverlays services and is not public API."
    }
}
