import Foundation
import FenrirNativeShared

extension Settings {
    enum LayerBoundary {
        static let storage = "Live settings storage stays behind Settings services and is not public API."
    }
}
