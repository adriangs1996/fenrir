import Foundation
import FenrirNativeShared

extension PaneGrid {
    enum LayerBoundary {
        static let terminalBytes = "PaneGrid hosts TerminalViewport but does not subscribe to pane streams."
    }
}
