# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Fenrir (for example via API key or ChatGPT auth supported by Codex).
- Fenrir starts the server via `codex app-server` per session.
